# 기여 방법

변경은 GitHub 이슈로 범위와 수용 기준을 먼저 기록한 뒤 feature 브랜치와 Pull Request로 제안합니다.

## 작업 원칙

1. 하나의 이슈에서 검토 가능한 한 가지 목적을 다룹니다.
2. 실행 기반, 기능과 테스트, 문서를 가능한 한 별도 커밋으로 나눕니다.
3. 커밋 메시지는 `type(scope): 요약` 형식을 사용합니다.
4. PR은 `main`을 대상으로 하고 관련 이슈를 연결합니다.
5. 변경 범위에 맞는 검사와 `python3 scripts/check_public_repo.py`를 실행합니다.

## 공개 안전 원칙

- 비밀값, 개인·운영 환경 설정, 원본 운영 데이터를 커밋하지 않습니다.
- 실제 endpoint, 상세 지역 식별자, 내부 저장 구조를 추가하지 않습니다.
- Agent, 데이터 처리, 모델의 비공개 핵심 구현을 추가하지 않습니다.
- 예제 수치와 응답은 `DUMMY` 또는 예시임을 명확히 표시합니다.
